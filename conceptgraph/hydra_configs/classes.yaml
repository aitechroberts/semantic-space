defaults:
  - base_paths

# Class vocabulary for yolo_sam backend. Ignored when segmentation_backend=sam_auto.
classes_file: ${repo_root}/conceptgraph/scannet200_classes.txt
bg_classes : ["wall", "floor", "ceiling"]
skip_bg: !!bool False