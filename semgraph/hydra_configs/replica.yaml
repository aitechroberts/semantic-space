defaults:
  - base_paths

dataset_root: ${data_root}/replica
dataset_config: ${repo_root}/semgraph/dataset/dataconfigs/replica/replica.yaml
scene_id: apartment1
render_camera_path: ${repo_root}/semgraph/dataset/dataconfigs/replica/replica_room0.json